name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  flowversion-conformance:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workflow-graph/packages/flowversion-conformance
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: workflow-graph/packages/flowversion-conformance/package-lock.json
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm test
      
      - name: Validate fixtures
        run: npm run validate

  workflow-graph-adapter:
    runs-on: ubuntu-latest
    needs: flowversion-conformance
    defaults:
      run:
        working-directory: workflow-graph/packages/workflow-graph-adapter
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install conformance toolkit
        run: |
          cd ../flowversion-conformance
          npm ci
      
      - name: Run adapter conformance
        run: |
          cd ../flowversion-conformance
          npm run test:adapter -- --config ../workflow-graph-adapter/conformance/conformance.config.json

  cpo-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Check SQL file syntax (basic)
        run: |
          # Basic syntax check - verify all SQL files are valid UTF-8
          find cpo/sql -name '*.sql' -exec file {} \; | grep -v 'UTF-8\|ASCII' && exit 1 || true
          echo "SQL files syntax check passed"
      
      - name: Verify STATUS.json structure
        run: |
          python3 -c "
          import json
          with open('cpo/STATUS.json') as f:
              data = json.load(f)
          assert 'phases' in data, 'STATUS.json missing phases'
          assert 'operational_mantra' in data, 'STATUS.json missing operational_mantra'
          for phase in ['P1','P2','P3','P4','P5','P6','P7']:
              assert phase in data['phases'], f'STATUS.json missing {phase}'
              assert data['phases'][phase]['state'] == 'complete', f'{phase} not complete'
          print('STATUS.json validation passed')
          "

  # CPO full pipeline requires PostgreSQL - run on demand or in separate workflow
  # cpo-pipeline:
  #   runs-on: ubuntu-latest
  #   services:
  #     postgres:
  #       image: postgres:15
  #       env:
  #         POSTGRES_PASSWORD: postgres
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5
  #       ports:
  #         - 5432:5432
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Run P7 pipeline
  #       env:
  #         DATABASE_URL: postgres://postgres:postgres@localhost:5432/postgres
  #       run: |
  #         cd cpo
  #         ./scripts/p7_ci_pipeline.sh
